#include <stdio.h>

int find_s(int level) {
    switch (level) {
        case 1;
        case 2
    }
}

int main() {
    int start, end;

    scanf("%d %d", &start, &end);

    // s accel, decel
    float s_accel = (1.5 * 1.5) / (2 * 0.5);
    float s_decel = (1.5 * 1.5) / (2 * 0.5);

    // t ช่วงเร่ง ชะลอ
    float t_accel = 1.5 / 0.5
    float t_decel = 1.5 / 0.5

    // s คงที่
    float s_sruise = 
    return 0;
}